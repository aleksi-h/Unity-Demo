using UnityEngine;
using System.Collections;

public class Constants {
    //public static int INITIAL_GRID_SIZE = 2;

    //these values come directly from the ground plane's position (0,0). if the ground is moved in scene view, these values have to be modified accordingly.
    public static int PLAYABLE_AREA_MIN_X = -60;
    public static int PLAYABLE_AREA_MAX_X = 60;
    public static int PLAYABLE_AREA_MIN_Z = -60;
    public static int PLAYABLE_AREA_MAX_Z = 60;
}
